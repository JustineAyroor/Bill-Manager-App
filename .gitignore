# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
*.db
# Virtual environments
.venv
.env

# Bill Import v2 (RAG pipeline) - regenerable index, not a source of truth.
# Rebuild anytime from approved Invoice/Allocation data via scripts/rebuild_vectorstore.py
data/vectorstore/

# Eval run history - local test-run data, not source; regenerated by
# eval/run_eval.py and by approving bill-import jobs.
eval/history.jsonl

# One-off local DB snapshot taken before a migration - not source.
tmobile.db.bak_before_n8n9
